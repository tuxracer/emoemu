/** Worker bundle filename, resolved relative to the built bundle at runtime */
export const KITTY_ENCODE_WORKER_FILENAME = './kittyEncodeWorker.js';

/** Max recycled transfer buffers kept around (in-flight + pending + spares) */
export const TRANSFER_POOL_MAX = 4;
