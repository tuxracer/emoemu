/** How long buffered chunks may sit in memory before an automatic flush */
export const FLUSH_INTERVAL_MS = 250;

/** Buffered chunk count that triggers an immediate flush */
export const FLUSH_MAX_CHUNKS = 256;
